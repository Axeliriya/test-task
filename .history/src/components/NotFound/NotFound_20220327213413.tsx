import styles from './NotFound.module.scss';
import {} from './';

export const App = ({ users, error, isLoading }: AppProps): JSX.Element => {
  return (
    <div>
      <img src={} alt="" />
    </div>
  );
};
